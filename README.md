# python-scraper-code
this repository contains an example code to scrape comments from target Indian medical YouTube channels and search queries. It uses scrapetube (video discovery) + youtube-comment-downloader (comment extraction).  With no API key required.
